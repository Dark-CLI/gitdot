-- Custom Keybinds
-- Vim-style navigation, gap control, custom utilities
-- Ported from: UserConfigs/UserKeybinds.conf (20 custom keybinds)

-- Phase 2: This file will contain all custom keybinds
-- For now, placeholder to prevent load errors

-- Examples (to be expanded in Phase 2):
-- hl.bind("SUPER + h", hl.dsp.window.focus({ direction = "left" }))
-- hl.bind("SUPER + =", function() gap_control.increment() end)
