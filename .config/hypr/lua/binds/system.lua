-- System Keybinds
-- Window control, workspace navigation, audio, brightness, etc.
-- Ported from: configs/Keybinds.conf (145 default keybinds)

-- Phase 2: This file will contain all default system keybinds
-- For now, placeholder to prevent load errors

-- Examples (to be expanded in Phase 2):
-- hl.bind("SUPER + Q", hl.dsp.window.close())
-- hl.bind("SUPER + F", hl.dsp.window.fullscreen())
-- hl.bind("SUPER + 1", hl.dsp.workspace.goToWorkspace({ id = 1 }))
