-- Window Rules
-- Window-specific configurations and behaviors
-- Ported from: configs/WindowRules.conf (112 rules) + UserConfigs/WindowRules.conf (4 custom rules)

-- Phase 2: This file will contain all window rules
-- For now, placeholder to prevent load errors

-- Examples (to be expanded in Phase 2):
-- hl.window_rule({
--   match = { class = "firefox" },
--   opacity = 0.95
-- })
-- hl.window_rule({
--   match = { class = "DropdownTerminal" },
--   float = true,
--   animation = "none"
-- })
